# Vercel hosts ONLY the static Hub (AgentEye_Hub.html + /assets). The API + all
# source runs on Railway, so keep everything else out of the Vercel deployment.
# (vercel.json is read as config; AgentEye_Hub.html and assets/ are NOT ignored.)

node_modules
server.js
package.json
package-lock.json
railway.json
workspace
appgen
cfn
library
website
generated
claude-quickstart
.agenteye_prompts
.auth
.git
.claude
*.md
*.bat
*.lnk
*.db
# never upload secrets to the frontend host
.env
.env.*
